{
  "$schema": "../../schemas/app-user-test-scenario-v2.schema.json",
  "schemaVersion": "2.0",
  "key": "auth.scenario.password-sign-in.password-entry-sign-in-locked-out",
  "runtimeEntityId": "B798DDBA2243CE291BA837CC173EDAB8",
  "name": "Password Sign-In Is Locked Out",
  "summary": "The user submits password credentials for a locked account, no session is established, and the client receives a lockout-specific result that can drive recovery or alternate authentication guidance.",
  "version": 2,
  "maturity": "proposed",
  "categoryKey": "password-sign-in",
  "startViewKey": "auth.continue.email.password",
  "action": { "id": "sign-in", "finder": "action:sign-in" },
  "preconditions": {
    "expression": "session-capability == anonymous and the identified durable identity is password-enabled and locked out",
    "state": {
      "ensureUserExists": "set",
      "userHasEmail": "set",
      "hasPassword": "set",
      "emailConfirmed": "set",
      "twoFactorEnabled": "notset",
      "isAccountDisabled": "notset",
      "isLockedOut": "set",
      "hasLastLogin": "notset",
      "isAnonymous": "notset"
    }
  },
  "inputs": [
    { "finder": "field:password", "name": "Password", "valueType": "secret-reference", "required": true, "value": "user.password", "description": "Valid generated password submitted for a test identity whose lockout state has been established by the setup manager." }
  ],
  "serverInteraction": {
    "required": true,
    "intent": "Attempt password authentication, detect the account lockout state, and return a lockout-specific result without establishing a session.",
    "transitionKeys": ["auth.transition.password-sign-in.locked-out"]
  },
  "expectedViewKey": "auth.sign-in.locked-out",
  "expectedVisibleFinders": ["action:forgot-password", "action:try-again", "action:change-email"],
  "postconditions": {
    "expression": "the durable identity remains locked out, no successful login is recorded, and auth.sign-in.locked-out is visible",
    "state": {
      "ensureUserExists": "set",
      "hasPassword": "set",
      "isAccountDisabled": "notset",
      "isLockedOut": "set",
      "hasLastLogin": "notset"
    }
  },
  "expectedAuthLogEvents": ["PasswordAuthenticationStarted", "PasswordAuthenticationFailed"],
  "evidenceRequirements": ["server", "web", "android", "ios"],
  "sourceReferences": [
    { "sourceType": "code", "reference": "src/LagoVista.AspNetCore.Identity/Managers/SigninManager.cs", "note": "SignInResult.IsLockedOut returns AUTH030 and records PasswordAuthenticationFailed without establishing a session." },
    { "sourceType": "code", "reference": "src/LagoVista.UserAdmin/Authentication/Flows/PasswordLoginFlowHandler.cs", "note": "AUTH030 results emit auth.transition.password-sign-in.locked-out." },
    { "sourceType": "test", "reference": "tests/LagoVista.UserAdmin.Auth.Tests/PasswordLoginFlowIntegrationTests.cs", "note": "LockedOut_Should_ReturnLockoutResult_And_RecordStartedThenFailed is the runtime integration proof." },
    { "sourceType": "auth-view", "reference": "auth.sign-in.locked-out", "note": "Canonical lockout surface is implemented and conformance-verified on web and mobile." },
    { "sourceType": "decision", "reference": "ChatGPT conversation 2026-08-09", "note": "The executable setup explicitly materializes the Identity lockout state so this scenario can run independently without prior failed-login attempts." }
  ],
  "progress": { "presentation": "complete", "implementation": "complete", "tests": "not-started" }
}
