{
  "$schema": "../../schemas/app-user-test-scenario-v2.schema.json",
  "schemaVersion": "2.0",
  "key": "auth.scenario.password-sign-in.password-entry-sign-in-success",
  "name": "Password Sign-In Succeeds",
  "summary": "The user submits valid password credentials, the server authenticates the durable identity, establishes a normal session, and returns the resolved landing destination.",
  "version": 1,
  "maturity": "proposed",
  "categoryKey": "password-sign-in",
  "startViewKey": "auth.continue.email.password",
  "action": { "id": "sign-in", "finder": "action:sign-in" },
  "preconditions": { "expression": "session-capability == anonymous and the submitted credentials identify an active password-enabled durable identity" },
  "inputs": [
    { "finder": "field:password", "name": "Password", "valueType": "secret-reference", "required": true, "description": "Valid password supplied through the test runner's secure settings." }
  ],
  "serverInteraction": {
    "required": true,
    "intent": "Authenticate the identified durable identity with the submitted password, establish the application session, and resolve the landing destination.",
    "transitionKeys": ["auth.transition.password-sign-in.success"]
  },
  "expectedViewKey": "app.home",
  "postconditions": { "expression": "session-capability == authenticated-normal and the resolved application landing destination is active" },
  "expectedAuthLogEvents": ["PasswordAuthenticationStarted", "PasswordAuthenticationSucceeded"],
  "evidenceRequirements": ["server", "web", "android", "ios"],
  "sourceReferences": [
    { "sourceType": "code", "reference": "src/LagoVista.AspNetCore.Identity/Managers/SigninManager.cs", "note": "PasswordSignInAsync completes successful authentication through CompleteSignInToAppAsync and records PasswordAuthenticationSucceeded." },
    { "sourceType": "code", "reference": "src/LagoVista.UserAdmin/Authentication/Flows/PasswordLoginFlowHandler.cs", "note": "Successful manager results emit auth.transition.password-sign-in.success." },
    { "sourceType": "test", "reference": "tests/LagoVista.UserAdmin.Auth.Tests/PasswordLoginFlowIntegrationTests.cs", "note": "SuccessfulLogin_Should_ReturnAuthenticated_And_RecordStartedThenSucceeded is the runtime integration proof." },
    { "sourceType": "decision", "reference": "ChatGPT conversation 2026-08-09", "note": "Implementation review confirms the generated proxy, REST endpoint, AuthenticationFlowService, typed handler and sign-in manager path for successful password sign-in." }
  ],
  "progress": { "presentation": "complete", "implementation": "complete", "tests": "not-started" }
}
