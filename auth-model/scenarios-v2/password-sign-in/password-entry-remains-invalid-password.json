{
  "$schema": "../../schemas/app-user-test-scenario-v2.schema.json",
  "schemaVersion": "2.0",
  "key": "auth.scenario.password-sign-in.password-entry-remains-invalid-password",
  "name": "Password Entry Remains After Invalid Password",
  "summary": "Deprecated pilot scenario retained for history. Invalid-password and unknown-account outcomes are now reconciled under auth.scenario.password-sign-in.password-entry-sign-in-rejected.",
  "version": 1,
  "maturity": "deprecated",
  "categoryKey": "password-sign-in",
  "startViewKey": "auth.continue.email.password",
  "action": {
    "id": "sign-in",
    "finder": "action:sign-in"
  },
  "preconditions": {
    "expression": "session-capability == anonymous and the identified account is active but the submitted password is invalid"
  },
  "inputs": [
    {
      "finder": "field:password",
      "name": "Password",
      "valueType": "secret-reference",
      "required": true,
      "description": "Invalid password supplied through the test runner's secure settings."
    }
  ],
  "serverInteraction": {
    "required": true,
    "intent": "Historical pilot for rejected password authentication.",
    "transitionKeys": [
      "auth.transition.password.establish-session"
    ]
  },
  "expectedViewKey": "auth.sign-in.unable",
  "expectedVisibleFinders": [
    "status:validation-error"
  ],
  "postconditions": {
    "expression": "session-capability == anonymous"
  },
  "expectedAuthLogEvents": [
    "PasswordAuthenticationStarted",
    "PasswordAuthenticationFailed"
  ],
  "evidenceRequirements": [
    "server",
    "web",
    "android",
    "ios"
  ],
  "sourceReferences": [
    {
      "sourceType": "discussion",
      "reference": "ChatGPT conversation 2026-07-31",
      "note": "Original invalid-password pilot path."
    },
    {
      "sourceType": "decision",
      "reference": "ChatGPT conversation 2026-08-09",
      "note": "Superseded by the non-enumerating rejected sign-in scenario, which groups invalid-password and unknown-account permutations."
    }
  ],
  "progress": {
    "presentation": "not-started",
    "implementation": "not-started",
    "tests": "not-started"
  }
}
