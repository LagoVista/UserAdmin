{
  "$schema": "../../schemas/app-user-test-scenario-v2.schema.json",
  "schemaVersion": "2.0",
  "key": "auth.scenario.password-sign-in.password-entry-sign-in-mfa-required",
  "runtimeEntityId": "6F3D1A7B42C84E7DB9135F0A6C2E8B41",
  "name": "Password Sign-In Requires TOTP",
  "summary": "The user submits valid password credentials for a durable identity whose only available MFA method is TOTP; the server returns the generic MFA-required outcome and the client routes directly to the canonical TOTP continuation surface.",
  "version": 1,
  "maturity": "proposed",
  "categoryKey": "password-sign-in",
  "startViewKey": "auth.continue.email.password",
  "action": { "id": "sign-in", "finder": "action:sign-in" },
  "preconditions": {
    "expression": "session-capability == anonymous and an active password-enabled durable identity has TOTP available and no Passkey available for MFA",
    "state": {
      "ensureUserExists": "set",
      "userHasEmail": "set",
      "userHasFirstName": "set",
      "userHasLastName": "set",
      "hasPassword": "set",
      "emailConfirmed": "set",
      "belongsToOrg": "set",
      "twoFactorEnabled": "set",
      "hasTOTP": "set",
      "hasPasskey": "notset",
      "isAccountDisabled": "notset",
      "isLockedOut": "notset",
      "accessFailedCount": 0,
      "isAnonymous": "notset",
      "showWelcome": "notset"
    }
  },
  "inputs": [
    { "finder": "field:email", "name": "Email", "valueType": "string", "required": true, "value": "user.email", "description": "Prepared test-user email resolved by the server before the runner plan is returned." },
    { "finder": "field:password", "name": "Password", "valueType": "secret-reference", "required": true, "value": "user.password", "description": "Valid password generated by the test setup manager and resolved by the server before the runner plan is returned." }
  ],
  "serverInteraction": {
    "required": true,
    "intent": "Submit the password through the normal password-sign-in flow and receive the generic MFA-required server outcome; the one-element available MFA provider set selects TOTP directly.",
    "transitionKeys": ["auth.transition.password-sign-in.mfa-required"]
  },
  "expectedViewKey": "auth.continue.totp",
  "postconditions": {
    "expression": "the password proof has succeeded, no authenticated application session is active, and the client has routed the TOTP-only result to auth.continue.totp",
    "state": {
      "ensureUserExists": "set",
      "hasPassword": "set",
      "emailConfirmed": "set",
      "belongsToOrg": "set",
      "twoFactorEnabled": "set",
      "hasTOTP": "set",
      "hasPasskey": "notset",
      "isAccountDisabled": "notset",
      "isLockedOut": "notset",
      "accessFailedCount": 0
    }
  },
  "expectedAuthLogEvents": ["PasswordAuthenticationStarted"],
  "evidenceRequirements": ["web", "android", "ios"],
  "sourceReferences": [
    { "sourceType": "code", "reference": "src/LagoVista.UserAdmin/Authentication/Flows/PasswordLoginFlowHandler.cs", "note": "The server returns generic MFA Required with source-supported provider availability." },
    { "sourceType": "code", "reference": "softwarelogistics/nuviot-ui-shared:user/email-login-password/email-login-password.component.ts", "note": "Angular Web routes TOTP-only MFA metadata to auth.continue.totp." },
    { "sourceType": "code", "reference": "nuviot/vtm-client:src/platform/auth/auth-context.tsx", "note": "React Native converts the TOTP-only response to mfa-totp before persisting any authenticated session." },
    { "sourceType": "code", "reference": "nuviot/vtm-client:src/features/authentication/nuviot-account/screens/continue-email-password-screen.tsx", "note": "React Native routes mfa-totp to auth.continue.totp." },
    { "sourceType": "decision", "reference": "auth-model/CLIENT-RESPONSE-ROUTING.md", "note": "A one-element availableMfaProviders list routes directly to that method; provider-specific routing does not require a distinct C# transition." }
  ],
  "progress": { "presentation": "complete", "implementation": "complete", "tests": "complete" }
}
