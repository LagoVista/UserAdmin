{
  "$schema": "../../schemas/app-user-test-scenario-v2.schema.json",
  "schemaVersion": "2.0",
  "key": "auth.scenario.session.sign-out-success",
  "runtimeEntityId": "7A4D1F9C2E6B43A58D90C1E2F3A4B5C6",
  "name": "Sign Out",
  "summary": "An authenticated user chooses Sign Out, the current client session is terminated, local authentication state is cleared, and the client returns to the Welcome surface.",
  "version": 1,
  "maturity": "proposed",
  "categoryKey": "session-management",
  "startViewKey": "app.home",
  "action": { "id": "sign-out", "finder": "action:sign-out" },
  "preconditions": {
    "expression": "the user has an active authenticated client session",
    "state": { "isUserLoggedIn": "set" }
  },
  "inputs": [],
  "serverInteraction": {
    "required": true,
    "intent": "Terminate only the current client session, revoking the current refresh token for token-backed clients or ending the application cookie session for cookie-backed clients.",
    "transitionKeys": ["auth.transition.session.sign-out-success"]
  },
  "expectedViewKey": "auth.welcome",
  "postconditions": {
    "expression": "the current client is anonymous, its local authentication state is cleared, and protected application state is no longer available through the terminated session",
    "state": { "isUserLoggedIn": "notset" }
  },
  "expectedAuthLogEvents": ["UserLogout"],
  "evidenceRequirements": ["server", "web", "android", "ios"],
  "sourceReferences": [
    { "sourceType": "code", "reference": "LagoVista/UserAdmin:src/LagoVista.UserAdmin/Authentication/Flows/SignOutFlowHandler.cs", "note": "Canonical typed handler revokes the supplied current refresh token when present, terminates the sign-in session and records UserLogout." },
    { "sourceType": "code", "reference": "LagoVista/UserAdminRest:src/LagoVista.UserAdmin.Rest/Rest/SignOutController.cs", "note": "Authorized POST /api/auth/signout delegates current user and organization context to AuthenticationFlowService." },
    { "sourceType": "code", "reference": "softwarelogistics/nuviot-ui-shared:user/logout/logout.component.ts", "note": "Angular Web invokes generated AuthClient.createSignout, clears remembered login seeds and performs a full navigation to auth.welcome." },
    { "sourceType": "code", "reference": "nuviot/vtm-client:src/platform/auth/auth-context.tsx", "note": "React Native sends the stored current refresh token through generated AuthClient.createSignout before clearing local session state and navigating to auth.welcome." },
    { "sourceType": "test", "reference": "auth.test-binding.session-sign-out", "note": "Authored integration proof covers cookie-backed session termination, token-backed current refresh-token revocation, canonical transition and UserLogout audit evidence." },
    { "sourceType": "decision", "reference": "ChatGPT conversation 2026-08-19", "note": "Sign Out is one atomic user action. No dedicated confirmation or completion AuthView is required; the action is host-owned and the canonical destination is auth.welcome." }
  ],
  "progress": { "presentation": "complete", "implementation": "complete", "tests": "complete" }
}
