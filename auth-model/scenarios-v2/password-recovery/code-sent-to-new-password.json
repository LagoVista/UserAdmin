{
  "$schema": "../../schemas/app-user-test-scenario-v2.schema.json",
  "schemaVersion": "2.0",
  "key": "auth.scenario.password-recovery.code-sent-to-new-password",
  "name": "Recovery Code Accepted",
  "summary": "The anonymous user submits the valid six-digit recovery code, the server consumes the code and establishes password-reset authority, and the client advances to the new-password view.",
  "version": 1,
  "maturity": "proposed",
  "categoryKey": "password-recovery",
  "startViewKey": "auth.password.reset.sent",
  "action": {
    "id": "verify-recovery-code",
    "finder": "action:verify-recovery-code"
  },
  "preconditions": {
    "expression": "session-capability == anonymous and auth.password.reset.sent is visible for a recoverable known account"
  },
  "inputs": [
    {
      "finder": "field:recovery-code",
      "name": "Recovery Code",
      "valueType": "secret-reference",
      "required": true,
      "description": "Six-digit password recovery code received through the verified email channel."
    }
  ],
  "serverInteraction": {
    "required": true,
    "intent": "Verify and consume the six-digit recovery code and establish reset authority without exposing the password-reset token to the user.",
    "transitionKeys": [
      "auth.transition.password-recovery.code-accepted"
    ]
  },
  "expectedViewKey": "auth.password.reset.complete",
  "postconditions": {
    "expression": "session-capability == anonymous and auth.password.reset.complete is visible with password-reset authority established internally"
  },
  "expectedAuthLogEvents": [
    "PasswordRecoveryCodeVerified"
  ],
  "evidenceRequirements": [
    "server",
    "web",
    "android",
    "ios"
  ],
  "sourceReferences": [
    {
      "sourceType": "code",
      "reference": "src/LagoVista.UserAdmin/Managers/PasswordManager.cs",
      "note": "VerifyPasswordResetCodeAsync verifies the six-digit code, consumes it, generates the password-reset token, and returns reset authority to the application flow."
    },
    {
      "sourceType": "decision",
      "reference": "ChatGPT conversation 2026-08-09",
      "note": "Successful code verification is the point where Password Recovery Success diverges from Password Recovery Rejected."
    }
  ],
  "progress": {
    "presentation": "complete",
    "implementation": "not-started",
    "tests": "not-started"
  }
}
