{
  "$schema": "../../schemas/app-user-test-scenario-v2.schema.json",
  "schemaVersion": "2.0",
  "key": "auth.scenario.totp-sign-in.verify-rejected",
  "runtimeEntityId": "B93367A9F1034894BA8A096098D8F11F",
  "name": "TOTP Sign-In Could Not Authenticate",
  "summary": "An identified returning user submits a TOTP code that is not accepted; no authenticated session is created and the client presents a dedicated error surface with Try Again and Start Over actions.",
  "version": 1,
  "maturity": "proposed",
  "categoryKey": "totp-sign-in",
  "startViewKey": "auth.continue.totp",
  "action": { "id": "verify", "finder": "action:verify" },
  "preconditions": {
    "expression": "session-capability == anonymous and an active durable identity with confirmed email and enrolled TOTP is prepared for sign-in",
    "state": {
      "ensureUserExists": "set",
      "userHasEmail": "set",
      "emailConfirmed": "set",
      "belongsToOrg": "set",
      "twoFactorEnabled": "set",
      "hasTOTP": "set",
      "isAccountDisabled": "notset",
      "isLockedOut": "notset",
      "isUserLoggedIn": "notset"
    }
  },
  "inputs": [
    { "finder": "field:email", "name": "Email", "valueType": "string", "required": true, "value": "user.email", "description": "Prepared test-user email resolved by the server before the runner plan is returned." },
    { "finder": "field:verification-code", "name": "Verification Code", "valueType": "secret-reference", "required": true, "value": "user.invalid-totp", "description": "Deliberately invalid current TOTP proof generated by deterministic test setup for the enrolled authenticator." }
  ],
  "serverInteraction": {
    "required": true,
    "intent": "Resolve the existing durable identity, reject the submitted TOTP proof without establishing a session, and return the canonical rejected TOTP sign-in outcome.",
    "transitionKeys": ["auth.transition.totp-sign-in.rejected"]
  },
  "expectedViewKey": "auth.continue.totp.unable",
  "expectedVisibleFinders": ["status:authentication-error", "action:try-again", "action:start-over"],
  "postconditions": {
    "expression": "the durable identity remains active with TOTP enabled, no successful login is recorded, no authenticated session is established, and the dedicated unable-to-authenticate view is visible",
    "state": {
      "ensureUserExists": "set",
      "emailConfirmed": "set",
      "belongsToOrg": "set",
      "twoFactorEnabled": "set",
      "hasTOTP": "set",
      "isAccountDisabled": "notset",
      "isLockedOut": "notset",
      "isUserLoggedIn": "notset"
    }
  },
  "expectedAuthLogEvents": ["TotpVerifyStart", "TotpVerifyFailed"],
  "evidenceRequirements": ["server", "web", "android", "ios"],
  "sourceReferences": [
    { "sourceType": "code", "reference": "src/LagoVista.UserAdmin/Managers/AppUserMfaManager.cs", "note": "VerifyTotpAsync emits TotpVerifyFailed and returns a failed result for an invalid or replayed TOTP proof." },
    { "sourceType": "auth-view", "reference": "auth.continue.totp.unable", "note": "Dedicated public failure surface provides Try Again and Start Over without disclosing internal rejection details." },
    { "sourceType": "decision", "reference": "ChatGPT conversation 2026-08-19", "note": "TOTP rejection is modeled as a distinct behavior because successful authentication and rejected proof have materially different session state and next actions." }
  ],
  "progress": { "presentation": "complete", "implementation": "in-progress", "tests": "not-started" }
}
