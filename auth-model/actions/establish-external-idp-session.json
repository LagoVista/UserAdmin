{
  "$schema": "../schemas/action.schema.json",
  "schemaVersion": "1.0",
  "key": "auth.action.external-idp.establish-session",
  "name": "Establish External Provider Session",
  "summary": "Establishes the normal application session for the already-resolved durable identity after valid external-provider proof and required home context are present.",
  "version": 1,
  "maturity": "proposed",
  "requiredInputs": [{ "key": "durable-identity-reference", "valueType": "reference", "required": true }],
  "guards": [
    { "expression": "durable identity is resolved and active", "failureCode": "durable-identity-not-ready" },
    { "expression": "credential-proof == oauth-proved", "failureCode": "oauth-proof-required" }
  ],
  "permittedMutations": ["auth.dimension.session-capability"],
  "requiredEffects": ["issue-normal-application-session", "record-external-provider-login"],
  "forbiddenEffects": ["create-durable-user", "change-provider-link", "grant-session-to-unresolved-identity"],
  "invariantKeys": ["auth.invariant.identity-resolution.no-duplicate-user"],
  "sourceReferences": [
    { "sourceType": "decision", "reference": "User Authentication", "note": "Approved policy requires provider-subject-first resolution and independent NuvIoT email proof before linking an unlinked provider to an existing durable user." },
    { "sourceType": "code", "reference": "src/LagoVista.UserAdmin.Models/Users/PendingIdentity.cs", "note": "PendingIdentity defines OAuthExternalLogin as a ceremony-only flow bucket." },
    { "sourceType": "decision", "reference": "ChatGPT conversation 2026-08-20", "note": "External IdP V2 reconciliation retains provider protocols as implementation details beneath provider-independent auth semantics." }
  ]
}
