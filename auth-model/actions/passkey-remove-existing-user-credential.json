{
  "$schema": "../schemas/action.schema.json",
  "schemaVersion": "1.0",
  "key": "auth.action.passkey.remove-existing-user-credential",
  "name": "Remove Existing User Passkey Credential",
  "summary": "Removes one passkey credential only when it belongs to the current resolved durable user and relying party.",
  "version": 1,
  "maturity": "proposed",
  "requiredInputs": [
    { "key": "credential-id", "valueType": "reference", "required": true, "description": "Credential selected from the current user's passkey list." }
  ],
  "guards": [
    { "expression": "selected credential belongs to the current durable identity and current relying party", "failureCode": "passkey-credential-not-owned" }
  ],
  "permittedMutations": [],
  "requiredEffects": ["delete-selected-passkey-credential-for-current-user"],
  "forbiddenEffects": ["delete-passkey-for-different-user", "change-durable-identity", "change-session-capability"],
  "invariantKeys": ["auth.invariant.transition.one-state-changing-action", "auth.invariant.transition.deterministic-post-state"],
  "sourceReferences": [
    { "sourceType": "code", "reference": "src/LagoVista.AspNetCore.Identity/Managers/AppUserPasskeyManager.cs", "note": "Existing passkey management validates credential ownership before removal." }
  ]
}
