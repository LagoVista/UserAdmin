{
  "$schema": "../schemas/linear-user-behavior-v2.schema.json",
  "schemaVersion": "2.0",
  "key": "auth.behavior.password.sign-in-passkey-required",
  "name": "Password Sign-In Passkey Required",
  "summary": "A user enters through Welcome, chooses email authentication, submits valid password credentials for an account whose only available MFA method is Passkey, receives the generic MFA-required server outcome, and the client routes directly to the existing Passkey Sign In flow.",
  "version": 1,
  "maturity": "proposed",
  "categoryKey": "password-sign-in",
  "scenarioKeys": [
    "auth.scenario.password-sign-in.welcome-to-email-entry",
    "auth.scenario.password-sign-in.email-entry-to-password-entry",
    "auth.scenario.password-sign-in.password-entry-sign-in-passkey-required"
  ],
  "entryViewKey": "auth.welcome",
  "finalViewKey": "auth.continue.passkey",
  "finalOutcome": "The password proof is accepted, no authenticated application session is issued yet, and Passkey-only client response routing places the user on the canonical passkey continuation surface; the existing Passkey Sign In category owns the remaining ceremony.",
  "supportedPlatforms": ["server", "web", "android", "ios"],
  "sourceReferences": [
    { "sourceType": "auth-view", "reference": "auth.continue.passkey", "note": "Canonical Passkey continuation surface is the direct destination when Passkey is the only available MFA method." },
    { "sourceType": "decision", "reference": "auth-model/CLIENT-RESPONSE-ROUTING.md", "note": "A one-element Passkey provider set routes directly to auth.continue.passkey; users with both TOTP and Passkey route to auth.select.mfa-type instead." },
    { "sourceType": "code", "reference": "src/LagoVista.AspNetCore.Identity/Managers/SigninManager.cs", "note": "The server remains responsible only for the generic MFA-required outcome; provider availability is response metadata used by the client routing layer." }
  ],
  "progress": { "scenarios": "complete", "presentation": "complete", "implementation": "in-progress", "tests": "complete" }
}
