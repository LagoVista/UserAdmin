name: Read only auth scenarios

on:
  push:
    branches: [master]
    paths:
      - .github/workflows/readonly-auth-scenarios.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply patch
        run: python3 .github/scripts/patch-readonly-auth-scenarios.py
      - name: Check patch
        run: git diff --check
      - name: Commit patch
        run: |
          git config user.name "Auth Model Patch Bot"
          git config user.email "auth-model-patch-bot@users.noreply.github.com"
          git rm .github/workflows/readonly-auth-scenarios.yml
          git rm .github/scripts/patch-readonly-auth-scenarios.py
          git add -A
          git commit -m "make canonical auth scenarios read only"
          git push origin HEAD:master
